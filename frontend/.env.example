# Copy to .env.local and fill in values
# Development: Vite proxy handles /api → Railway, so no CORS issues
VITE_API_URL=/api/v1

# Production (Vercel/Railway): set to full backend URL
# VITE_API_URL=https://web-production-cea7d.up.railway.app/api/v1
